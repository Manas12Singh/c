#include <stdbool.h>
#include <string.h>

bool isValid(char *s)
{
    int n=strlen(s);
    int j=0;
    int *ptr =j;
    isclose(s,0,ptr,n)
    return;
}

int main()
{
    char *s = "({})()";
    printf("%d", isValid(s));
    return 0;
}
